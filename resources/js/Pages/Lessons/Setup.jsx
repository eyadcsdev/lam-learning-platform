import { SetupExperience } from "@/components/lesson/setup/setup-experience"

export default function Setup({ technology, lesson, is_unlocked }) {
  return <SetupExperience technology={technology} />
}
